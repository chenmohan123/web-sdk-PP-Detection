# 公开 npm 0.2.0 示例验收

核验日期：2026-09-08（Asia/Shanghai）。分支基于发布提交 `32b057b825037802ed025f1740409b1529d97fc5`，属于单 SDK 示例层。

六种示例固定升级为公开 `web-sdk-pp-detection@0.2.0`，同步 README、锁文件和已有版本断言。pnpm 只新增当前自有包精确版本的 `minimumReleaseAgeExclude`；其他依赖解析不变。SDK 与模型源码没有变更。

实际 registry 包 SHA-512 为 `sha512-5z58eNg6mtbLqWQ4MKmnBgidkx7Ik7N0m1lxroC1qjxDknZvgZ/O7AZo0TRpkG3Su8wApf5JGcHvM9/e6EiDig==`。下载后摘要一致；公开 provenance 和 `gitHead` 指向上面的发布提交。ESM、Worker 及全局入口 source map 中 24 个 SDK 源文件与发布提交一致，仅归一化换行。来源核验不包含 Sigstore 签名验签。

## 验证

- 修改前后标准检查均为 required 18 通过、0 失败、4 远程跳过；证据为 [before](2026-09-08-examples-0.2.0-standard-before.json)、[after](2026-09-08-examples-0.2.0-standard-after.json)。
- `pnpm examples:smoke`：12 项通过，包含 React、Vue、Vanilla Vite、微信 web-view、Vanilla 和 CDN 六种入口的重复点击、取消及官方 PicoDet WASM 推理。四个构建型示例独立安装公开包并构建。
- 独立公开包消费者完成 TypeScript 类型检查、Vite 构建、main/Worker 微型 ONNX 推理、模型缓存查询/清理、取消和重复释放，且保留其他模型缓存。见 [consumer.json](2026-09-08-examples-0.2.0-consumer.json)。
- 示例与文档契约、格式及差异检查通过。原始日志位于本地 `.tmp/examples-0.2.0/`，不提交。

环境为 Windows 10.0.26200、Chromium 151.0.7922.34、Node 24.16.0、ONNX Runtime Web 1.27.0、WASM。六入口测试隔离模型网络并使用本地官方模型字节，静态入口的 SDK 文件来自实际 npm 安装；该结果不证明外部 CDN/模型源网络可用性，也不新增物理 GPU、移动端或微信宿主兼容结论。
