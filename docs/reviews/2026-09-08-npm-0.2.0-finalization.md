# Detection 0.2.0 发布定稿核验

核验日期：2026-09-08（北京时间）。本报告是当日发布定稿阶段的本地证据快照。基线提交为 `5adeb3963a61f722d7570139beec7631df6cc351`，分支为 `codex/prepare-npm-0-2-0`；采集时，版本准备及本轮定稿变更尚未提交。之后的提交、合并、部署或发布不会改变这份快照描述的时间范围。

分层：单 SDK 的 npm 发布准备。依据门户 `standards/v1/README.md`、SDK runtime 契约、文档发布契约、性能契约和 release checklist。候选阶段的原始结果、环境与旧 tarball 哈希继续保存在[历史候选核验](2026-09-08-npm-0.2.0.md)，不作为本轮最终包验证证据。

## 定稿范围

- 中英根 README、npm README、API 和性能说明移除候选阶段流程文案，安装命令固定 `web-sdk-pp-detection@0.2.0`，保留新增 API 的版本边界。
- CHANGELOG 定稿为 `0.2.0（2026-09-08）`，同步现有版本断言。
- [中文发布说明](../zh-CN/release-0.2.0.md)和[等价英文说明](../en/release-0.2.0.md)自包含模型、来源、许可、后端、变更和验证边界。模型 manifest 默认来源为 Hugging Face；Demo 来源选择默认设为 ModelScope，明确两个配置层的区别。
- 原候选阶段报告改为带日期的历史快照，保留原日志、消费者结果与包哈希；发布流程移入发布准备记录。

相对基线提交，`packages/sdk/src` 的唯一改动仍为 `CURRENT_SDK_VERSION` 从 0.1.1 同步到 0.2.0；本轮定稿没有新增运行时或 Demo 行为修改。模型二进制、manifest 来源、公开示例依赖及相关 lockfile 均未修改。六种公开示例继续固定 npm 0.1.1，其升级应在真实 registry 0.2.0 校验后进行。

## 发布准备状态

此快照将 0.2.0 记录为待 npm 发布的最终准备版本。本地已准备正式版本文档和最终 tarball；本轮没有执行提交、push、标签、npm publish、GitHub Release 或远端设置修改。根与 npm README 的 0.2.0 安装命令用于该版本的正式文档，不是 npm 发布成功的证据。前次候选快照记录的公开 npm 版本为 0.1.1，本轮没有重新查询 registry。

后续发布流程：

1. 审查并提交定稿差异，通过 PR 的 required CI 后合并到受保护的 main；刷新 About、Rulesets、必需 CI 与 Pages 的提交关联部署证据。
2. npm 发布前复核 npm Trusted Publisher 与 `environment: npm`，确保不可变版本标签指向已验证且属于 main 的提交。按发布范围执行标签、Trusted Publishing 和 GitHub Release；Release 使用本仓库双语正式说明。
3. 发布后读取 npm 0.2.0 元数据，核对实际 tarball 的 integrity、provenance 和标签提交。registry 包重新打包产生不同归档哈希时，应核对包内内容与对应构建来源，不能只沿用本地哈希。
4. 在 registry 证据通过后，将 `examples/{react,vue,vanilla-vite,wechat-webview}/package.json`、`examples/{cdn,vanilla}/index.html`、六种示例 README、相关 lockfile 和断言升级到固定 0.2.0，基于真实 npm 包重跑独立安装、类型检查、构建和浏览器推理、取消、释放 smoke。

## 本轮验证

环境：Windows_NT 10.0.26200 x64，Node v24.16.0，pnpm 11.16.0，Playwright 1.62.1，Chromium 151.0.7922.34，ONNX Runtime Web 1.27.0。执行依赖命令时仅在进程内设置 `pnpm_config_verify_deps_before_run=false`；沙箱不能运行依赖 shim 时，复用宿主机工具。没有升级依赖。

| 检查                                               | 结果                                                                                                                                                                         | 证据                                                                                                                                       |
| -------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| 修改前、修改后 SDK 标准检查                        | required 18 通过、0 失败、4 跳过；recommended 3 通过；locally-compliant                                                                                                      | [before.json](2026-09-08-npm-0.2.0-finalization-standard-before.json)、[after.json](2026-09-08-npm-0.2.0-finalization-standard-after.json) |
| `node scripts/verify-release.mjs --release v0.2.0` | 退出码 0；模型大小、SHA-256、历史模型报告及版本字符串验证通过；仅校验字符串，未创建标签                                                                                      | `.tmp/release-0.2.0-final/verify-release.log`                                                                                              |
| 完整 `pnpm run verify`，由上述命令调用             | format；文档 5 项；示例契约 10 项；发布、安全和仓库契约 20 项；基准契约 6 项；parity 14 项；SDK 和 Demo lint；SDK typecheck；SDK 19 文件、132 项单测；SDK build 全部通过     | 同上                                                                                                                                       |
| 包浏览器 smoke，由上述命令调用                     | 4 项通过：独立打包安装、ESM 与声明消费、Vite 构建、Worker 资源与 WASM 推理、CDN global 分离、包内无 ONNX                                                                     | 同上                                                                                                                                       |
| 最终 tarball 独立安装                              | 固定 TypeScript 5.9.3、Vite 7.3.6；实际从新 tarball 安装 0.2.0                                                                                                               | `.tmp/release-0.2.0-final/consumer-install.log`                                                                                            |
| 最终包公开类型与构建                               | `tsc --noEmit` 和 `vite build --target es2022` 均退出码 0；消费模型身份缓存 API、`ModelCache.scope/list()`、`PPDetectionLoadTimings.modelSource` 和运行时版本/环境类型       | `.tmp/release-0.2.0-final/consumer-typecheck.log`、`consumer-build.log`                                                                    |
| 最终包浏览器消费                                   | 模型缓存查询和清理正确且保留其他模型；缺省模型返回 `INVALID_MANIFEST`；main/Worker WASM 各完成一次确定性 ONNX 推理、取消和重复释放；来源、ORT 版本与环境字段正确；无外部请求 | [consumer.json](2026-09-08-npm-0.2.0-finalization-consumer.json)，内含最终 tarball SHA-256                                                 |
| 包内容与工作区比对                                 | README、根 LICENSE、7 个 dist 文件逐字节一致；package.json 解析后完全一致；tarball 精确包含 10 个预期文件，无 ONNX                                                           | [artifact.json](2026-09-08-npm-0.2.0-finalization-artifact.json)                                                                           |

Demo 35 项测试、Demo typecheck/build 和额外 runtime/cache 浏览器 smoke 的前次结果见历史候选报告。本轮只变更文档及 CHANGELOG 断言，未重复 Demo 行为验证。完整发布校验中的 Demo lint 本轮重新执行。

标准检查从门户根目录执行：

```powershell
pnpm sdk:check -- --repo F:/git/00_chenmohan/github/web-sdk-PP-Detection --format json --out F:/git/00_chenmohan/github/web-sdk-PP-Detection/docs/reviews/2026-09-08-npm-0.2.0-finalization-standard-after.json
```

独立消费者与原始日志保留在 `.tmp/release-0.2.0-final/`，按仓库规则忽略。先使用 `pnpm --dir packages/sdk pack --pack-destination ../../.tmp/release-0.2.0-final` 打包，再运行 `pnpm --dir .tmp/release-0.2.0-final/consumer install --ignore-workspace --ignore-scripts`、`pnpm --dir .tmp/release-0.2.0-final/consumer exec tsc --noEmit`、`pnpm --dir .tmp/release-0.2.0-final/consumer exec vite build --target es2022`、`node .tmp/release-0.2.0-final/verify-artifact.mjs` 和 `node .tmp/release-0.2.0-final/consumer/browser-smoke.mjs`。消费者只从最终安装包加载 SDK，微型 ONNX fixture 来自现有测试目录。

## 最终保留包

- 路径：`F:/git/00_chenmohan/github/web-sdk-PP-Detection/.tmp/release-0.2.0-final/web-sdk-pp-detection-0.2.0.tgz`
- 大小：1,364,878 字节。
- SHA-256：`0b19a390c6fc127ace546d3068622afcb6f876d0ceddba9206f3fc0b49b38753`。
- SHA-512/SRI：`sha512-17fmbCXepZwtftueUwQXYlpb2SvK0LouPVlwZSg0BOl41zm4KODREBaKwM3XoqyP52ekDAbQnFUyR1GOEvy0MA==`。
- 原候选包 SHA-256 `103804fa91ec480a607bb96a480f9fc02b2aea2a512ffa7c4694760b7b8b9ea4` 及旧归档原位保留，二者不是同一 tarball。

此最终包哈希对应上述独立消费者验证。之后若更改包内文档、代码或依赖，应重新打包并验证新归档。

## 证据限制

本地标准检查有 4 项远端规则跳过，因此只能称为 locally-compliant。没有刷新远端治理、npm 发布或线上部署状态。

真实 PicoDet 1.0.1 FP32 七张 fixture 的 WASM/WebGPU 证据仍为 2026-08-30；完整发布校验只复核现有模型文件和历史报告的一致性，本轮没有重新运行真实模型 GPU 基准。确定性微型 ONNX fixture 只验证包与生命周期，不构成 PicoDet 精度或吞吐基准。没有新增移动端、微信 WebView、Safari、Firefox 或物理 GPU 兼容性结论。
