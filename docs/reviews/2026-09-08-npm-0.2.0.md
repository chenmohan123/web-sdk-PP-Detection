# Detection 0.2.0 本地候选核验

本报告是 2026-09-08 11:51（北京时间）的候选阶段历史快照，后续定稿与最终包证据见[发布定稿核验](2026-09-08-npm-0.2.0-finalization.md)。以下状态和结果仅描述该次核验。范围：单 SDK npm 发布准备。基线提交 `5adeb3963a61f722d7570139beec7631df6cc351`；本地分支 `codex/prepare-npm-0-2-0`，该快照时变更尚未提交。该次核验开始时工作区干净。仓库内没有额外 AGENTS.md，遵循门户标准入口、SDK/runtime 契约、文档发布契约与 release checklist。

## 结果与候选范围

本地 0.2.0 候选完成版本与文档准备，完整发布校验、Demo、浏览器缓存及独立 tarball 消费者验证通过。公开 npm 与六种公开示例仍为 0.1.1；未修改示例依赖、相关 lockfile、模型资产或运行时行为。`packages/sdk/src` 的唯一差异是 `CURRENT_SDK_VERSION` 从 0.1.1 同步为 0.2.0。

同步根包、SDK 包、运行时常量、SDK manifest 和现有版本断言；将 Unreleased 整理为明确“候选，尚未发布”的 0.2.0 CHANGELOG；补齐[中文发布说明](../zh-CN/release-0.2.0.md)及[等价英文说明](../en/release-0.2.0.md)。中英根 README、npm README、API、性能文档明确新 API 从 0.2.0 起提供，以及当前候选尚未公开。修正了根与 npm README 中“省略 model 使用默认模型”的错误，实际工厂要求 `model` 或 `manifest`，两者均缺省时为 `INVALID_MANIFEST`。

本轮没有 git 提交、push、标签、PR、npm publish、GitHub Release 或远端设置写入。候选阶段的发布步骤曾列在发布说明中，定稿时迁入发布准备记录。

## 验证证据

本机环境为 Windows_NT 10.0.26200 x64、Node v24.16.0、pnpm 11.16.0、Playwright 1.62.1、Chromium 151.0.7922.34、ONNX Runtime Web 1.27.0。pnpm 执行前仅进程级设置 `pnpm_config_verify_deps_before_run=false`；沙箱无法运行依赖 shim 时使用已自动批准的宿主机执行环境，未升级依赖。

| 项目                                                                                        | 本轮结果                                                                                                                                                 | 证据                                                               |
| ------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| 修改前标准检查                                                                              | required 18 通过、0 失败、4 跳过；recommended 3 通过；locally-compliant                                                                                  | [before.json](2026-09-08-npm-0.2.0-standard-before.json)           |
| 修改后标准检查                                                                              | required 18 通过、0 失败、4 跳过；recommended 3 通过；locally-compliant                                                                                  | [after.json](2026-09-08-npm-0.2.0-standard-after.json)             |
| `node scripts/verify-release.mjs --release v0.2.0`                                          | 退出码 0；模型大小/哈希与历史验证报告、版本标签字符串匹配；未创建标签                                                                                    | `.tmp/release-0.2.0/verify-release.log`                            |
| 完整 `pnpm run verify`（由上一命令调用）                                                    | format、文档 5 项、示例契约 10 项、发布/安全/仓库契约 20 项、基准契约 6 项、parity 14 项、lint、SDK typecheck、SDK 单测 19 文件/132 项和 SDK build 通过  | 同上                                                               |
| package smoke（由完整发布校验调用）                                                         | 4 项通过：独立 tarball 安装、ESM/声明/Vite 构建、Worker 资源及 WASM 推理、CDN global 分离、包内无 ONNX                                                   | 同上                                                               |
| `pnpm --filter @ppdetection/demo typecheck` 与 `build`                                      | 均退出码 0                                                                                                                                               | 本轮命令输出；生产构建位于 `apps/demo/dist`                        |
| `pnpm --filter @ppdetection/demo test`                                                      | 35 项通过：0.2.0 显示、缓存清理/遮罩、初始化耗时、视频/摄像头会话与竞态、界面 smoke                                                                      | `.tmp/release-0.2.0/demo-tests.log`                                |
| `pnpm exec playwright test tests/browser/runtime.spec.ts tests/browser/model-cache.spec.ts` | 6 项通过；3 项真实模型/GPU 门禁按范围跳过                                                                                                                | `.tmp/release-0.2.0/browser-smoke.log`                             |
| 本地 0.2.0 独立消费者安装、类型检查、Vite 构建                                              | 成功；SDK 指向实际 tarball，TypeScript 5.9.3、Vite 7.3.6 固定版本                                                                                        | `.tmp/release-0.2.0/consumer-install.log`、`consumer-build.log`    |
| 独立消费者浏览器验证                                                                        | 新增模型身份缓存 API、保留其他模型缓存、缺省模型报错；main/Worker WASM 各完成一次确定性 ONNX 推理、取消和重复释放；来源/ORT/环境字段正确；无外部网络请求 | [consumer.json](2026-09-08-npm-0.2.0-consumer.json)                |
| 模型文件                                                                                    | 23,243,834 字节；SHA-256 与 manifest 完全一致                                                                                                            | `0397bb449689d1bf57dfcb8849b3ddaa1c8962e1e63e533bd97d265908a428a1` |
| 包与工作区一致性                                                                            | 已安装候选 README 与 7 个 dist 文件逐字节一致；package.json 解析后完全一致                                                                               | 本轮逐文件检查；pnpm pack 只调整 JSON 字段顺序                     |

原始日志和独立消费者工程保留在本地 `.tmp/release-0.2.0/`（按仓库规则忽略，不进入版本控制）。消费者运行脚本为 `.tmp/release-0.2.0/consumer/browser-smoke.mjs`；它只从候选安装包加载 SDK，从现有测试目录读取确定性 tiny ONNX fixture。该 fixture 验证包装与生命周期，不构成 PicoDet 精度/吞吐基准。

标准检查命令在门户根目录运行，修改前后分别保存 JSON：

```powershell
$env:pnpm_config_verify_deps_before_run = 'false'
pnpm sdk:check -- --repo F:/git/00_chenmohan/github/web-sdk-PP-Detection --format json --out F:/git/00_chenmohan/github/web-sdk-PP-Detection/docs/reviews/2026-09-08-npm-0.2.0-standard-after.json
```

独立消费者使用 `pnpm --dir .tmp/release-0.2.0/consumer install --ignore-workspace --ignore-scripts`，再执行 `pnpm --dir .tmp/release-0.2.0/consumer exec tsc --noEmit`、`pnpm --dir .tmp/release-0.2.0/consumer exec vite build --target es2022` 与 `node .tmp/release-0.2.0/consumer/browser-smoke.mjs`。工作区及公开示例的 lockfile 保持不变。

## 保留的候选包

- 路径：`F:/git/00_chenmohan/github/web-sdk-PP-Detection/.tmp/release-0.2.0/web-sdk-pp-detection-0.2.0.tgz`
- 大小：1,364,931 字节。
- SHA-256：`103804fa91ec480a607bb96a480f9fc02b2aea2a512ffa7c4694760b7b8b9ea4`。
- SHA-512/SRI：`sha512-zf/r+UwWXm8B3Zq+VcZSF1id+JTzwTXHs26bZ7sFnKQWTWpd7flVMmZrArrXKqLBa5PnK9rq3wKwro3WMNz7hA==`。
- 内容共 10 个文件：ESM、global、Worker 三组 JS/source map、类型声明、LICENSE、package.json、双语 README；无 ONNX。

此历史哈希仅标识该次保留的本地候选包；最终待发包使用发布定稿核验报告中的新哈希，本记录不作为最终包验证证据。

## 候选快照时的后续事项

- 按单独授权范围审查/提交候选变更，更新候选标记及 CHANGELOG 断言，并重新校验最终待发包。
- 刷新主分支/标签 Ruleset、必需 CI、About、Demo/Pages 部署证据，以及 npm Trusted Publisher 与 `environment: npm` 的匹配。标准检查中的 4 项远端规则跳过，因此结论仅为 locally-compliant。
- 在明确授权后才创建标签、运行 Trusted Publishing、创建/核对 GitHub Release；之后核对 registry tarball、provenance 和标签提交，再更新公开示例至真实 0.2.0。
- 本轮不重新运行完整真实 PicoDet WASM/GPU 基准；真实模型七张 fixture 证据仍为 2026-08-30。没有新增物理 GPU、移动端、微信 WebView、Safari 或 Firefox 兼容性结论。
