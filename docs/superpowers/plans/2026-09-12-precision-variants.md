# 多精度模型分发与对比实施计划

用户已授权：生成 PicoDet 与 PP-YOLOE 的 FP16，对比 FP32/FP16/W8A32；体积收益独立计入，通过识别与运行验证后分发并上线。

架构：单 SDK 模型变体扩展。复用当前 SDK 0.3.1 推理契约，FP32 输入输出不变，检测数量 INT32。权重版本独立，来源仅 ModelScope 与 Hugging Face，默认 ModelScope；自动精度继续 FP32。新增模型版本 PicoDet 1.0.2、PP-YOLOE 0.1.1。

- [x] 读取标准和既有 FP16/W8A32 证据，保存修改前检查。
- [x] 生成 PicoDet FP16（Cast_5 保持 FP32）及 PP-YOLOE FP16（ReduceMean 保持 FP32），完整 ONNX 校验。
- [x] 两模型 × 三精度 × 两后端 × 三轮，同一64图、相同SDK字节、串行运行。记录AP、匹配框差异、下载体积、初始化和热耗时；AP下降超过0.5先停查，不发布数值异常变体。
- [x] 将通过的转换过程整理为可重复工具及行为测试，重新生成并核对SHA-256。
- [x] Demo 添加W8A32显示及选择，清单驱动组合限制，保留自动FP32和来源默认；覆盖切换取消、状态标签和窄屏。
- [x] 完成统一对比报告，保存失败/成功与原始数据；手机和峰值内存只写实际证据范围。
- [x] 新版本模型卡、许可、清单准备完成后上传两Hub，记录不可变提交并回下载核验SHA-256，保护旧资产。
- [x] 更新SDK仓库清单、Demo和文档，完成相关测试、类型、lint、构建、生产预览真实推理与规范检查。
- [ ] 提交中文变更，通过PR和CI发布Pages，必要时复用F:/github-runner；核验线上双模型三精度及默认来源。

验证按模型和后端分别判断。可选变体即使不加速，也可凭下载体积优势发布。未测设备不虚构兼容承诺；不因缺少手机重测阻塞已授权且识别通过的桌面验证版本。
